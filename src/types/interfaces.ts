export default interface Note {
  id: string;
  title: string;
  description: string;
  isPinned: boolean;
  isFavorite: boolean;
  backgroundColor: string;
  textColor: string;
  createdAt: string;
}
